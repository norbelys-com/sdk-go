/*
Norbelys API

The **Norbelys API** is a single, predictable REST surface for cold email and outreach — people, senders, programs, and sending all live behind the five patterns below. Developer-first and AI-first: every name is either already invented (Schema.org) or obvious.  ## Authentication  Every request authenticates with an **org-scoped API key**. Create one in **Settings → API keys** and send it as a bearer token:  ```http GET https://api.norbelys.com/v1/people Authorization: Bearer ak_live_… ```  Interactive agents may instead use OAuth 2.1 (see `/auth.md` and the `/.well-known/oauth-protected-resource` metadata).  ## Conventions  - **Base URL** — `https://api.norbelys.com/v1`. - **JSON in, JSON out.** Timestamps are ISO-8601 in UTC. - **Cursor pagination.** List endpoints take `limit` + `cursor` and return   `{ data, hasMore, nextCursor }` (offset-paged tables add `page` + `total`). - **Expansions.** Detail GETs take an `expand[]` query param to inline related   data (e.g. `GET /people/{id}?expand[]=timeline`) instead of extra calls. - **Soft deletes.** Anything that has been used is archived, never hard-deleted —   `DELETE` archives the resource and returns it.  ## Errors  Failures return the same envelope on every 4xx/5xx, with the matching HTTP status:  ```json { \"error\": { \"type\": \"invalid_request\", \"code\": \"invalid_param\",             \"message\": \"…\", \"hint\": \"…\", \"doc_url\": \"…\" } } ```  `type` is a broad, machine-routable category derived from the status; `code` is the stable machine contract you branch on (never the human `message`). See the `ApiError` schema.  ## Idempotency  Every `POST` accepts an optional **`Idempotency-Key`** header. Reuse the same key to replay the original result for 24h instead of re-executing — so a retried create can never double-charge or duplicate a record.  ## Rate limits & versioning  Abuse control is enforced at the edge; responses advertise the policy via the `RateLimit-Policy` header, and a `429` carries `Retry-After`. The API is versioned in the URL path (`/v1`). Breaking changes ship under a new version; a retiring surface is announced with `Deprecation` + `Sunset` response headers at least 90 days ahead.

API version: 0.0.1
*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.

package norbelys

import (
	"encoding/json"
	"bytes"
	"fmt"
)

// checks if the SenderRemoveResult type satisfies the MappedNullable interface at compile time
var _ MappedNullable = &SenderRemoveResult{}

// SenderRemoveResult struct for SenderRemoveResult
type SenderRemoveResult struct {
	// \"deleted\" (no history) or \"archived\" (history kept).
	Outcome string `json:"outcome"`
	Sender NullableSender `json:"sender"`
}

type _SenderRemoveResult SenderRemoveResult

// NewSenderRemoveResult instantiates a new SenderRemoveResult object
// This constructor will assign default values to properties that have it defined,
// and makes sure properties required by API are set, but the set of arguments
// will change when the set of required properties is changed
func NewSenderRemoveResult(outcome string, sender NullableSender) *SenderRemoveResult {
	this := SenderRemoveResult{}
	this.Outcome = outcome
	this.Sender = sender
	return &this
}

// NewSenderRemoveResultWithDefaults instantiates a new SenderRemoveResult object
// This constructor will only assign default values to properties that have it defined,
// but it doesn't guarantee that properties required by API are set
func NewSenderRemoveResultWithDefaults() *SenderRemoveResult {
	this := SenderRemoveResult{}
	return &this
}

// GetOutcome returns the Outcome field value
func (o *SenderRemoveResult) GetOutcome() string {
	if o == nil {
		var ret string
		return ret
	}

	return o.Outcome
}

// GetOutcomeOk returns a tuple with the Outcome field value
// and a boolean to check if the value has been set.
func (o *SenderRemoveResult) GetOutcomeOk() (*string, bool) {
	if o == nil {
		return nil, false
	}
	return &o.Outcome, true
}

// SetOutcome sets field value
func (o *SenderRemoveResult) SetOutcome(v string) {
	o.Outcome = v
}

// GetSender returns the Sender field value
// If the value is explicit nil, the zero value for Sender will be returned
func (o *SenderRemoveResult) GetSender() Sender {
	if o == nil || o.Sender.Get() == nil {
		var ret Sender
		return ret
	}

	return *o.Sender.Get()
}

// GetSenderOk returns a tuple with the Sender field value
// and a boolean to check if the value has been set.
// NOTE: If the value is an explicit nil, `nil, true` will be returned
func (o *SenderRemoveResult) GetSenderOk() (*Sender, bool) {
	if o == nil {
		return nil, false
	}
	return o.Sender.Get(), o.Sender.IsSet()
}

// SetSender sets field value
func (o *SenderRemoveResult) SetSender(v Sender) {
	o.Sender.Set(&v)
}

func (o SenderRemoveResult) MarshalJSON() ([]byte, error) {
	toSerialize,err := o.ToMap()
	if err != nil {
		return []byte{}, err
	}
	return json.Marshal(toSerialize)
}

func (o SenderRemoveResult) ToMap() (map[string]interface{}, error) {
	toSerialize := map[string]interface{}{}
	toSerialize["outcome"] = o.Outcome
	toSerialize["sender"] = o.Sender.Get()
	return toSerialize, nil
}

func (o *SenderRemoveResult) UnmarshalJSON(data []byte) (err error) {
	// This validates that all required properties are included in the JSON object
	// by unmarshalling the object into a generic map with string keys and checking
	// that every required field exists as a key in the generic map.
	requiredProperties := []string{
		"outcome",
		"sender",
	}

	allProperties := make(map[string]interface{})

	err = json.Unmarshal(data, &allProperties)

	if err != nil {
		return err;
	}

	for _, requiredProperty := range(requiredProperties) {
		if _, exists := allProperties[requiredProperty]; !exists {
			return fmt.Errorf("no value given for required property %v", requiredProperty)
		}
	}

	varSenderRemoveResult := _SenderRemoveResult{}

	decoder := json.NewDecoder(bytes.NewReader(data))
	decoder.DisallowUnknownFields()
	err = decoder.Decode(&varSenderRemoveResult)

	if err != nil {
		return err
	}

	*o = SenderRemoveResult(varSenderRemoveResult)

	return err
}

type NullableSenderRemoveResult struct {
	value *SenderRemoveResult
	isSet bool
}

func (v NullableSenderRemoveResult) Get() *SenderRemoveResult {
	return v.value
}

func (v *NullableSenderRemoveResult) Set(val *SenderRemoveResult) {
	v.value = val
	v.isSet = true
}

func (v NullableSenderRemoveResult) IsSet() bool {
	return v.isSet
}

func (v *NullableSenderRemoveResult) Unset() {
	v.value = nil
	v.isSet = false
}

func NewNullableSenderRemoveResult(val *SenderRemoveResult) *NullableSenderRemoveResult {
	return &NullableSenderRemoveResult{value: val, isSet: true}
}

func (v NullableSenderRemoveResult) MarshalJSON() ([]byte, error) {
	return json.Marshal(v.value)
}

func (v *NullableSenderRemoveResult) UnmarshalJSON(src []byte) error {
	v.isSet = true
	return json.Unmarshal(src, &v.value)
}


