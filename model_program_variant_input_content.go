/*
Norbelys API

The **Norbelys API** is a single, predictable REST surface for cold email and outreach — people, senders, programs, and sending all live behind the five patterns below. Developer-first and AI-first: every name is either already invented (Schema.org) or obvious.  ## Authentication  Every request authenticates with an **org-scoped API key**. Create one in **Settings → API keys** and send it as a bearer token:  ```http GET https://api.norbelys.com/v1/people Authorization: Bearer ak_live_… ```  Interactive agents may instead use OAuth 2.1 (see `/auth.md` and the `/.well-known/oauth-protected-resource` metadata).  ## Conventions  - **Base URL** — `https://api.norbelys.com/v1`. - **JSON in, JSON out.** Timestamps are ISO-8601 in UTC. - **Cursor pagination.** List endpoints take `limit` + `cursor` and return   `{ data, hasMore, nextCursor }` (offset-paged tables add `page` + `total`). - **Expansions.** Detail GETs take an `expand[]` query param to inline related   data (e.g. `GET /people/{id}?expand[]=timeline`) instead of extra calls. - **Soft deletes.** Anything that has been used is archived, never hard-deleted —   `DELETE` archives the resource and returns it.  ## Errors  Failures return the same envelope on every 4xx/5xx, with the matching HTTP status:  ```json { \"error\": { \"type\": \"invalid_request\", \"code\": \"invalid_param\",             \"message\": \"…\", \"hint\": \"…\", \"doc_url\": \"…\" } } ```  `type` is a broad, machine-routable category derived from the status; `code` is the stable machine contract you branch on (never the human `message`). See the `Error` schema.  ## Idempotency  Every `POST` accepts an optional **`Idempotency-Key`** header. Reuse the same key to replay the original result for 24h instead of re-executing — so a retried create can never double-charge or duplicate a record.  ## Rate limits & versioning  Abuse control is enforced at the edge; responses advertise the policy via the `RateLimit-Policy` header, and a `429` carries `Retry-After`. The API is versioned in the URL path (`/v1`). Breaking changes ship under a new version; a retiring surface is announced with `Deprecation` + `Sunset` response headers at least 90 days ahead.

API version: 0.0.1
*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.

package norbelys

import (
	"encoding/json"
	"fmt"
)


// ProgramVariantInputContent The arm's email content (subject/html/text, optional AI directive).
type ProgramVariantInputContent struct {
	AiEmailVariantContent *AiEmailVariantContent
	StaticEmailVariantContent *StaticEmailVariantContent
}

// Unmarshal JSON data into any of the pointers in the struct
func (dst *ProgramVariantInputContent) UnmarshalJSON(data []byte) error {
	var err error
	// try to unmarshal JSON data into AiEmailVariantContent
	err = json.Unmarshal(data, &dst.AiEmailVariantContent);
	if err == nil {
		jsonAiEmailVariantContent, _ := json.Marshal(dst.AiEmailVariantContent)
		if string(jsonAiEmailVariantContent) == "{}" { // empty struct
			dst.AiEmailVariantContent = nil
		} else {
			return nil // data stored in dst.AiEmailVariantContent, return on the first match
		}
	} else {
		dst.AiEmailVariantContent = nil
	}

	// try to unmarshal JSON data into StaticEmailVariantContent
	err = json.Unmarshal(data, &dst.StaticEmailVariantContent);
	if err == nil {
		jsonStaticEmailVariantContent, _ := json.Marshal(dst.StaticEmailVariantContent)
		if string(jsonStaticEmailVariantContent) == "{}" { // empty struct
			dst.StaticEmailVariantContent = nil
		} else {
			return nil // data stored in dst.StaticEmailVariantContent, return on the first match
		}
	} else {
		dst.StaticEmailVariantContent = nil
	}

	return fmt.Errorf("data failed to match schemas in anyOf(ProgramVariantInputContent)")
}

// Marshal data from the first non-nil pointers in the struct to JSON
func (src *ProgramVariantInputContent) MarshalJSON() ([]byte, error) {
	if src.AiEmailVariantContent != nil {
		return json.Marshal(&src.AiEmailVariantContent)
	}

	if src.StaticEmailVariantContent != nil {
		return json.Marshal(&src.StaticEmailVariantContent)
	}

	return nil, nil // no data in anyOf schemas
}


type NullableProgramVariantInputContent struct {
	value *ProgramVariantInputContent
	isSet bool
}

func (v NullableProgramVariantInputContent) Get() *ProgramVariantInputContent {
	return v.value
}

func (v *NullableProgramVariantInputContent) Set(val *ProgramVariantInputContent) {
	v.value = val
	v.isSet = true
}

func (v NullableProgramVariantInputContent) IsSet() bool {
	return v.isSet
}

func (v *NullableProgramVariantInputContent) Unset() {
	v.value = nil
	v.isSet = false
}

func NewNullableProgramVariantInputContent(val *ProgramVariantInputContent) *NullableProgramVariantInputContent {
	return &NullableProgramVariantInputContent{value: val, isSet: true}
}

func (v NullableProgramVariantInputContent) MarshalJSON() ([]byte, error) {
	return json.Marshal(v.value)
}

func (v *NullableProgramVariantInputContent) UnmarshalJSON(src []byte) error {
	v.isSet = true
	return json.Unmarshal(src, &v.value)
}


