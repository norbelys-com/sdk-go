/*
Norbelys API

The **Norbelys API** is a single, predictable REST surface for cold email and outreach — people, senders, programs, and sending all live behind the five patterns below. Developer-first and AI-first: every name is either already invented (Schema.org) or obvious.  ## Authentication  Every request authenticates with an **org-scoped API key**. Create one in **Settings → API keys** and send it as a bearer token:  ```http GET https://api.norbelys.com/v1/people Authorization: Bearer ak_live_… ```  Interactive agents may instead use OAuth 2.1 (see `/auth.md` and the `/.well-known/oauth-protected-resource` metadata).  ## Conventions  - **Base URL** — `https://api.norbelys.com/v1`. - **JSON in, JSON out.** Timestamps are ISO-8601 in UTC. - **Cursor pagination.** List endpoints take `limit` + `cursor` and return   `{ data, hasMore, nextCursor }` (offset-paged tables add `page` + `total`). - **Expansions.** Detail GETs take an `expand[]` query param to inline related   data (e.g. `GET /people/{id}?expand[]=timeline`) instead of extra calls. - **Soft deletes.** Anything that has been used is archived, never hard-deleted —   `DELETE` archives the resource and returns it.  ## Errors  Failures return the same envelope on every 4xx/5xx, with the matching HTTP status:  ```json { \"error\": { \"type\": \"invalid_request\", \"code\": \"invalid_param\",             \"message\": \"…\", \"hint\": \"…\", \"doc_url\": \"…\" } } ```  `type` is a broad, machine-routable category derived from the status; `code` is the stable machine contract you branch on (never the human `message`). See the `Error` schema.  ## Idempotency  Every `POST` accepts an optional **`Idempotency-Key`** header. Reuse the same key to replay the original result for 24h instead of re-executing — so a retried create can never double-charge or duplicate a record.  ## Rate limits & versioning  Abuse control is enforced at the edge; responses advertise the policy via the `RateLimit-Policy` header, and a `429` carries `Retry-After`. The API is versioned in the URL path (`/v1`). Breaking changes ship under a new version; a retiring surface is announced with `Deprecation` + `Sunset` response headers at least 90 days ahead.

API version: 0.0.1
*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.

package norbelys

import (
	"encoding/json"
	"bytes"
	"fmt"
)

// checks if the WarmupTodayTally type satisfies the MappedNullable interface at compile time
var _ MappedNullable = &WarmupTodayTally{}

// WarmupTodayTally Today's warmup tally for this mailbox.
type WarmupTodayTally struct {
	// Warmup sends that failed today.
	Failed int32 `json:"failed"`
	// Warmup sends planned for today.
	Planned int32 `json:"planned"`
	// Warmup sends completed today.
	Sent int32 `json:"sent"`
}

type _WarmupTodayTally WarmupTodayTally

// NewWarmupTodayTally instantiates a new WarmupTodayTally object
// This constructor will assign default values to properties that have it defined,
// and makes sure properties required by API are set, but the set of arguments
// will change when the set of required properties is changed
func NewWarmupTodayTally(failed int32, planned int32, sent int32) *WarmupTodayTally {
	this := WarmupTodayTally{}
	this.Failed = failed
	this.Planned = planned
	this.Sent = sent
	return &this
}

// NewWarmupTodayTallyWithDefaults instantiates a new WarmupTodayTally object
// This constructor will only assign default values to properties that have it defined,
// but it doesn't guarantee that properties required by API are set
func NewWarmupTodayTallyWithDefaults() *WarmupTodayTally {
	this := WarmupTodayTally{}
	return &this
}

// GetFailed returns the Failed field value
func (o *WarmupTodayTally) GetFailed() int32 {
	if o == nil {
		var ret int32
		return ret
	}

	return o.Failed
}

// GetFailedOk returns a tuple with the Failed field value
// and a boolean to check if the value has been set.
func (o *WarmupTodayTally) GetFailedOk() (*int32, bool) {
	if o == nil {
		return nil, false
	}
	return &o.Failed, true
}

// SetFailed sets field value
func (o *WarmupTodayTally) SetFailed(v int32) {
	o.Failed = v
}

// GetPlanned returns the Planned field value
func (o *WarmupTodayTally) GetPlanned() int32 {
	if o == nil {
		var ret int32
		return ret
	}

	return o.Planned
}

// GetPlannedOk returns a tuple with the Planned field value
// and a boolean to check if the value has been set.
func (o *WarmupTodayTally) GetPlannedOk() (*int32, bool) {
	if o == nil {
		return nil, false
	}
	return &o.Planned, true
}

// SetPlanned sets field value
func (o *WarmupTodayTally) SetPlanned(v int32) {
	o.Planned = v
}

// GetSent returns the Sent field value
func (o *WarmupTodayTally) GetSent() int32 {
	if o == nil {
		var ret int32
		return ret
	}

	return o.Sent
}

// GetSentOk returns a tuple with the Sent field value
// and a boolean to check if the value has been set.
func (o *WarmupTodayTally) GetSentOk() (*int32, bool) {
	if o == nil {
		return nil, false
	}
	return &o.Sent, true
}

// SetSent sets field value
func (o *WarmupTodayTally) SetSent(v int32) {
	o.Sent = v
}

func (o WarmupTodayTally) MarshalJSON() ([]byte, error) {
	toSerialize,err := o.ToMap()
	if err != nil {
		return []byte{}, err
	}
	return json.Marshal(toSerialize)
}

func (o WarmupTodayTally) ToMap() (map[string]interface{}, error) {
	toSerialize := map[string]interface{}{}
	toSerialize["failed"] = o.Failed
	toSerialize["planned"] = o.Planned
	toSerialize["sent"] = o.Sent
	return toSerialize, nil
}

func (o *WarmupTodayTally) UnmarshalJSON(data []byte) (err error) {
	// This validates that all required properties are included in the JSON object
	// by unmarshalling the object into a generic map with string keys and checking
	// that every required field exists as a key in the generic map.
	requiredProperties := []string{
		"failed",
		"planned",
		"sent",
	}

	allProperties := make(map[string]interface{})

	err = json.Unmarshal(data, &allProperties)

	if err != nil {
		return err;
	}

	for _, requiredProperty := range(requiredProperties) {
		if _, exists := allProperties[requiredProperty]; !exists {
			return fmt.Errorf("no value given for required property %v", requiredProperty)
		}
	}

	varWarmupTodayTally := _WarmupTodayTally{}

	decoder := json.NewDecoder(bytes.NewReader(data))
	decoder.DisallowUnknownFields()
	err = decoder.Decode(&varWarmupTodayTally)

	if err != nil {
		return err
	}

	*o = WarmupTodayTally(varWarmupTodayTally)

	return err
}

type NullableWarmupTodayTally struct {
	value *WarmupTodayTally
	isSet bool
}

func (v NullableWarmupTodayTally) Get() *WarmupTodayTally {
	return v.value
}

func (v *NullableWarmupTodayTally) Set(val *WarmupTodayTally) {
	v.value = val
	v.isSet = true
}

func (v NullableWarmupTodayTally) IsSet() bool {
	return v.isSet
}

func (v *NullableWarmupTodayTally) Unset() {
	v.value = nil
	v.isSet = false
}

func NewNullableWarmupTodayTally(val *WarmupTodayTally) *NullableWarmupTodayTally {
	return &NullableWarmupTodayTally{value: val, isSet: true}
}

func (v NullableWarmupTodayTally) MarshalJSON() ([]byte, error) {
	return json.Marshal(v.value)
}

func (v *NullableWarmupTodayTally) UnmarshalJSON(src []byte) error {
	v.isSet = true
	return json.Unmarshal(src, &v.value)
}


